# Dependencies
node_modules/

# Session transfer leftovers
handoff/
HANDOFF.md
NEW-SESSION-PROMPT.txt
*.patch

# Large/incoming source archives are not committed — extract & import, then
# keep only the processed JSON under books/<id>/data/.
incoming/
*.zip
source_pdfs/
_staging/
*.pdf

# Python
.venv/
__pycache__/
*.pyc

# Consolidated-notes build intermediates (regenerate via scripts/)
build_consolidated/figures/
build_physics/figures/

# UPSC polity/geography sprint intermediates — _prelims_records.json is re-parsed
# from data/prelims.json by scripts/analyse_polity_geo_pyqs.py, _console.txt is its log
build_upsc_pg/_prelims_records.json
build_upsc_pg/_console.txt

# Scratch backups from tools/split-bpsc.js
*.bak
_site/
