# Dependencies
node_modules/

# Session transfer leftovers
handoff/
HANDOFF.md
NEW-SESSION-PROMPT.txt
*.patch

# Large/incoming source archives are not committed — extract & import, then
# keep only the processed JSON under books/<id>/data/.
incoming/
*.zip
source_pdfs/
_staging/
*.pdf

# Python
.venv/
__pycache__/
*.pyc

# Consolidated-notes build intermediates (regenerate via scripts/)
build_consolidated/figures/
build_physics/figures/

# Scratch backups from tools/split-bpsc.js
*.bak
_site/
